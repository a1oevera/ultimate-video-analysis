# Track A (this package) — coordinate-space possession logic, no GPU
numpy>=1.24
scipy>=1.10

# Track B (future CV pipeline) — needs a GPU, install when you start it:
# ultralytics          # YOLO
# supervision          # detection/tracking/annotation utilities (roboflow)
# umap-learn           # for TeamClassifier
# transformers         # SigLIP embeddings for team classification
# torch                # backend
# opencv-python        # homography, mosaic registration
# git+https://github.com/roboflow/sports.git   # homography + team classifier to lift
